# Shared Reports

This route serves publicly accessible shared report pages.

## Responsibilities

- Resolve saved report content by ID
- Render HTML responses safely for viewers outside the authenticated app shell
- Provide a bridge back into the main application context for follow-up analysis

## Direction

- Shared HTML reports remain supported for lightweight distribution
- Live MotherDuck Dives are the preferred path for rich, embedded visual workspaces
